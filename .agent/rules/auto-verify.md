---
trigger: always_on
---

Always perform autonomous verification. After writing code, the agent must launch the local server in OrbStack, use the built-in browser to verify the UI works, and fix any errors before reporting the task as complete.